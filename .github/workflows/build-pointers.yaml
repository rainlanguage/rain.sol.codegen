name: build-pointers
on: [push]
jobs:
  build-pointers:
    # Runs script/Build.sol (this repo has no CopyArtifacts.sol, so that step is
    # skipped) and asserts the committed generated sources still match.
    # secrets: inherit carries CACHIX_AUTH_TOKEN.
    uses: rainlanguage/rainix/.github/workflows/rainix-copy-artifacts.yaml@main
    secrets: inherit
